build-gradle 主要：
  1. 完成 app 和  readercomponent, sharecomponent, sharecomponentkotlin 的依赖 和  readercomponent, sharecomponet
    , sharecomponentkotlin 各自独立运行

  2. 在 运行的module 的 Application 中， 添加 (new ReaderAppLike()).onCreate();
                                            (new ShareApplike()).onCreate();
                                            (new KotlinApplike()).onCreate();  如下代码